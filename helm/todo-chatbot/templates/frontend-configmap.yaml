apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "todo-chatbot.fullname" . }}-frontend-config
  labels:
    {{- include "todo-chatbot.frontendLabels" . | nindent 4 }}
data:
  BETTER_AUTH_URL: {{ .Values.frontend.env.BETTER_AUTH_URL | quote }}
